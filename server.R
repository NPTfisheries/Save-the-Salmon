# SERVER
shinyServer(function(input, output) {
  
  
  popServer(id='Lolo Creek')
  popServer(id='Johnson Creek')
  
  # for(pop in populations) {
    # popServer(id = pop)
  # }
  
}) # close server 